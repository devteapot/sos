# The physical recovery gate needs a non-root way to restart only the durable
# authority. This permission is omitted from production user policy.
userdebug_or_eng(`set_prop(shell, sos_authority_recovery_probe_prop)')
